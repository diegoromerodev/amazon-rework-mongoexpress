extends layout

block content
    .container
        h1.form Deleting category: #{category.name}
        .delete-info
            p Name
            h2= category.name
        form(action="" method="post")
            button(type="submit") Delete category and products
        